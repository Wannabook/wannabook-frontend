import React from 'react';

export default class Signup extends React.Component {
  // TODO add redirection to "/" in case a user is already registered
  render() {
    return <div>Registration page</div>;
  }
}
